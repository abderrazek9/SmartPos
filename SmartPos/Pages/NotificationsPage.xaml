<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SmartPos.ViewModels"
             xmlns:models="clr-namespace:SmartPos.Models"
             x:Class="SmartPos.Pages.NotificationsPage"
             Title="Notifications"
             x:Name="ThisPage"
             >



    <ContentPage.Content>

        <StackLayout Padding="10">

            <Label 
  Text="اسحب لليسار للحذف  ←"
  FontSize="10"
  TextColor="Gray"
  HorizontalOptions="Center"
  Margin="5,4,0,10"/>

            <CollectionView ItemsSource="{Binding Notifications}" x:DataType="vm:NotificationsViewModel">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:NotificationModel">
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem Text="Delete"
                                               IconImageSource="delete.png"
                                               BackgroundColor="LightSalmon"
                                               Command="{Binding Source={x:Reference ThisPage}, Path=BindingContext.DeleteCommand}"
                                               CommandParameter="{Binding .}"/>
                                </SwipeItems>
                            </SwipeView.RightItems>
                <Grid Padding="10" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Label
            Text="{Binding Timestamp, StringFormat='{}{0:HH:mm dd/MM/yyyy}'}"
            FontSize="Small"
            Grid.Row="0"
            HorizontalOptions="End" />
                    <Label
            Text="{Binding Message}"
            FontSize="Medium"
            Grid.Row="1" />
                </Grid>
                        </SwipeView>
                    </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>